spring:
  datasource:
    url: jdbc:mysql://3.36.251.109:3306/zigzzang
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: vdongv1620
    hikari:
      maximum-pool-size: 20
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        show_sql: true
        format_sql: true
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
  sql:
    init:
      mode: always

logging:
  level:
    org.hibernate.SQL: debug


